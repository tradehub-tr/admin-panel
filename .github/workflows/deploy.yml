name: Deploy Admin Panel

on:
  push:
    branches: [ master, ahmet, ali, bora ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  # ── 1. Beta sunucuya deploy (sadece master) ──────────
  # master → beta.istoc.com/panel (otomatik)
  # rc.istoc.com/panel ve istoc.com/panel için Jenkins pipeline'ları kullanılır.
  deploy-beta:
    if: github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to BETA server via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 152.53.147.222
          username: root
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          port: 5055
          script: |
            set -e
            cd /opt/tradehub-beta/admin-panel
            git reset --hard origin/master
            git pull origin master
            cd /opt/tradehub-beta
            docker compose build --no-cache admin-panel-beta
            docker compose up -d --force-recreate admin-panel-beta
            echo "✅ Admin panel BETA deploy tamamlandı → https://beta.istoc.com/panel"

  # ── 2. GitHub Pages deploy (ahmet, ali, bora) ──────────
  deploy-pages:
    if: github.ref != 'refs/heads/master'
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      - name: Build for GitHub Pages
        working-directory: frontend
        run: npm run build
        env:
          GITHUB_PAGES: 'true'
          VITE_API_BASE: https://beta.istoc.com
          VITE_FRAPPE_BACKEND: https://beta.istoc.com
          VITE_FRAPPE_SOCKETIO: https://beta.istoc.com
          VITE_STOREFRONT_URL: https://beta.istoc.com

      - name: Copy index.html to 404.html for SPA routing
        run: cp frontend/dist/index.html frontend/dist/404.html

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: frontend/dist

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
