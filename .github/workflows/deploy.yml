name: Deploy Admin Panel

on:
  push:
    branches: [ master, ahmet, ali, bora ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  # ── 1. Alpha sunucuya deploy (sadece master) ──────────
  # master → alpha.istoc.com/panel (otomatik) — frontend build BURADA yapilir
  # beta/rc/istoc /panel için Jenkins pipeline'ları kullanılır.
  deploy-alpha:
    if: github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to ALPHA server via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 152.53.147.222
          username: root
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          port: 5055
          script: |
            set -e
            cd /opt/tradehub-alpha/admin-panel
            git reset --hard origin/master
            git pull origin master
            cd /opt/tradehub-alpha
            docker compose build --no-cache admin-panel-alpha
            docker compose up -d --force-recreate admin-panel-alpha
            echo "✅ Admin panel ALPHA deploy tamamlandı → https://alpha.istoc.com/panel"

  # ── 2. GitHub Pages deploy (ahmet, ali, bora) ──────────
  deploy-pages:
    if: github.ref != 'refs/heads/master'
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      - name: Build for GitHub Pages
        working-directory: frontend
        run: npm run build
        env:
          GITHUB_PAGES: 'true'
          VITE_API_BASE: https://alpha.istoc.com
          VITE_FRAPPE_BACKEND: https://alpha.istoc.com
          VITE_FRAPPE_SOCKETIO: https://alpha.istoc.com
          VITE_STOREFRONT_URL: https://alpha.istoc.com

      - name: Copy index.html to 404.html for SPA routing
        run: cp frontend/dist/index.html frontend/dist/404.html

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: frontend/dist

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
